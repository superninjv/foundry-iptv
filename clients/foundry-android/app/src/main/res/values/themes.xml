<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Leanback-compatible theme required for the LEANBACK_LAUNCHER intent filter.
        The Compose TV content overrides the visual style at runtime, but the
        manifest still needs a Leanback theme to pass the Play Store TV review.
    -->
    <style name="Theme.FoundryIptv" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorPrimary">#FF7E1F</item>
        <item name="android:colorAccent">#FF7E1F</item>
        <!-- Disable the default window title bar — Compose draws the full UI -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
